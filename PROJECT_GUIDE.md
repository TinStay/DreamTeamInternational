# DreamTeam Web — Project Guide

Single source of truth for this repo: what the app is, how it's built, and the rules every change must follow. **Living document** — update it whenever the architecture, conventions, or business facts change (e.g. after optimizations or new features).

## 1. What this app is

Marketing site for **DreamTeam** — an AI video production **company** (never call it an "agency") based in Sofia, Bulgaria, serving clients in Bulgaria and worldwide.

- Website: `https://dreamteam.video` (base URL for metadata, sitemap, JSON-LD)
- Email stays on the old domain: `info@dreamteam.technology` (Resend-verified — do not change without re-verifying)
- Services: AI Video Production (`ai-video`), Brand Mascots (`brand-mascots`), AI Images / Graphic Design (`ai-images`), Video & Image Automation (`ai-automation`)
- Training: individual lessons, Skool community, team workshops
- Business facts used in copy: individual quotes (price depends on complexity/length); delivery 5–12 **working days** with a **paid prioritization** option; tools chosen adaptively across AI/video models; clients get full commercial rights; fully remote workflow.

## 2. Tech stack

- **Next.js 16** (App Router, Turbopack) + **React 19** + **TypeScript (strict)**
  - ⚠️ Read `node_modules/next/dist/docs/` before using unfamiliar APIs — this version differs from training data. `params`/`searchParams` are **Promises** (must be awaited).
- **Tailwind CSS v4** (+ `tw-animate-css`), shadcn-style components on **@base-ui/react** + Radix primitives
- **motion** (`import { motion } from "motion/react"`) for animation — do NOT add `framer-motion`; it was consolidated away
- **Lenis** (`lenis`) for site-wide smooth scrolling — `components/smooth-scroll.tsx` mounts one instance in the root layout (`lerp` 0.06 — about a third of a second of glide, so a single wheel tick reads as motion without the page feeling like ice; `wheelMultiplier` 0.85, a wheel tick moves the page a little less than the browser would, so the pages read at a calmer pace — the client asked for a slower scroll; `autoRaf`, nested scrollers keep their own wheel, touch stays native, reduced motion turns it off) and puts it on `window.__lenis`; it also intercepts same-page hash links (`/bg#quote` from `/bg`) and scrolls them clear of the header. **Programmatic scrolls go through `lib/smooth-scroll.ts`** (`scrollToElement` — pass the header clearance as `offset`, Lenis ignores `scroll-margin` — and `scrollToY`), never `scrollIntoView` / `window.scrollTo` directly; `html:not(.lenis)` keeps native `scroll-behavior: smooth` as the fallback and Lenis's own stylesheet is inlined in `globals.css`. **Scroll-linked motion gets exactly one easing**: `useScrollEased(source, spring)` (same module) hands back the raw scroll progress while the page's own scrolling is already smooth — Lenis gliding a fine pointer's wheel (no reduced motion), or **any coarse pointer** (touch scrolls natively and continuously; a spring on top of it only trailed the finger and doubled the frames worked — the stage follows the finger 1:1 on phones now) — and the caller's `useSpring` of it only when a fine pointer scrolls natively (wheel steps). The journey scenes, the reviews conveyor and the projects showcase all go through it. **Scroll progress of an element comes from `useFlowProgress(ref, range, deps)`** (same module), never from motion's `useScroll({ target })`: that one measures its target on every scroll event — a forced style + layout flush each time, and the home page had two dozen of them, which on a phone was most of the frame — while `useFlowProgress` measures the element's document position once and again on a resize or whenever the body's height changes (a ResizeObserver), and derives the progress from the shared window scroll value; `range(top, height, vh)` returns the two scroll positions the progress runs between (motion's `["start end", "start 0.7"]` = `[top − vh, top − 0.7·vh]`). Only for in-flow elements — the journey's markers and the showcase section use it; the reviews runway and the process wave (inside pinned boxes) still use motion's `useScroll`. Two motion rules that bit here: a `useTransform` is recomputed at most once per frame step, so never feed one from a change handler or from another derived value that can change later in the same step (feed it raw scroll / spring values, or sum on the next frame's pre-update as the journey does); and JS-driven transforms / opacity on big elements need `will-change` to stay off the paint path — the moving journey boxes / parts, the review track and the morph carry it, and the big decorative glows are plain radial gradients (no `filter: blur()` — the GPU re-blurs a moving layer every frame)
- **Resend** for contact/inquiry email; **PostHog** (`disable_scroll_properties: true` — the scroll-depth listener read the document's scroll height on every scroll event, a forced layout in the middle of the scroll-linked animations and the single hottest thing on a phone; the projects stage is `ph-no-capture`, so the session recorder ignores its per-frame style mutations) + **Vercel Analytics**; **next-themes** (dark default); **OpenAI conversion pixel** (`oaiq`, loader in `src/app/layout.tsx`, `lead_created` fired via `src/lib/openai-pixel.ts`)
- No styled-components / CSS-in-JS / `style jsx` — Tailwind classes or shared CSS in `src/app/globals.css` only

## 3. Structure (where things live)

| Path | Purpose |
| --- | --- |
| `src/app/[lang]/…` | Locale routes (`bg`, `en`): home, `contact`, `portfolio`, `projects(/[slug])`, `services(/[slug])`, `training(/[slug])`, `privacy`, `terms` |
| `src/components/contact-process-layout.tsx` (+ `contact-quote-cta.tsx`) | The `/contact` page: breadcrumbs, then a **quote strip** up top (`ContactQuoteCta` — one line, `contactPage.quoteLead`, and the site's quote pill on `#quote`, which the smooth scroll lands clear of the header), the contact form + details (`ContactSection`), then the **home page's service cards + step form** (`QuoteFormSection`, its `id="quote"` — the same wizard, step 0 = the cards) under the form, and the process section |
| `src/app/api/contact/route.ts` | Contact endpoint → Resend (validation, honeypot `website` field, rate limit) |
| `src/app/layout.tsx` | Root layout: metadata, Organization/WebSite JSON-LD, providers, `defaultTheme="dark"` |
| `src/app/sitemap.ts`, `robots.ts`, `opengraph-image.tsx` | SEO endpoints; robots explicitly allows AI crawlers |
| `src/app/home-page.tsx` | Home composition — self-contained sections stacked in order, in two scroll journeys around the projects stage: `ScrollJourney overlapFirst={false} leaveLast` [hero → services + quote wizard] · `ProjectsShowcase` (its wrapper overlaps the wizard's tail by `-mt-[45svh]`, so the cinema headline is already rising while the wizard's parts leave) · `ScrollJourney` [stats → reviews → training → contact → process → FAQ]. The hero (`hero-section.tsx`) owns the partners marquee (bottom strip over the video), marks its headline (`title`), subtitle + CTAs and the strip as `JourneyItem`s so they leave as the wizard arrives, and its primary CTA jumps to `#quote`; the wizard's scene carries `hold={0.45}` (its reading room, see `scroll-journey.tsx`) so it never starts leaving while someone is still on a short step; the hero has `hold={0}` (it is pinned from the top of the page). There is no separate services section any more — the service cards are the wizard's first step. The full portfolio lives on `/portfolio` (no teaser on the home page — `ui/hero-3.tsx` is an unused marquee primitive kept for reuse) |
| `src/components/quote-form/` + `src/lib/quote-form/constants.ts` | Services + multi-step quote wizard (home, right after the hero). Step 0 = the four service cards (`steps/service-step.tsx`, the same `ServiceCard`s as `/services`, heading = `services.title1/2` + subtitle) — each card's primary pill (`services.quoteCta`) picks the service (`QuoteFormData.service`, keys = `SERVICE_ICONS` via `getServiceKey`) and opens its own flow from `QUOTE_FLOWS`: **video** `script → goal → video → details`, **images** `imagesSpecs` (how many · resolution 1080p/2K/4K · aspect ratios, multi) → `imagesBrief` (description + product / example images + links) → `imagesTiming` (deadline + where they'll be used), **mascot** `mascotStyle` (2D/3D · style · where it lives) → `mascotBrief` → `mascotTiming` (deliverables + deadline), **automation** `automationScope` (what to produce · monthly volume) → `automationBrief` (what they have + how it should work) → `automationTiming` (destinations + start date) — every flow ends on the shared `contact` step (`quoteStepsFor(service)` builds the list; Back from a flow's first step returns to the cards, the heading switches to `quoteForm.title1/2` + a `{service} · step N of M` eyebrow). Building blocks: `steps/option-group.tsx` (a labelled grid of `OptionCard`s, radio or multi), `steps/brief-step.tsx` (textarea + upload + links, copy per flow), `steps/timing-step.tsx` (calendar + notes + an aside group); the video steps keep their bespoke components. Option keys/icons/limits and `isQuoteStepValid` live in the constants file, labels in the dictionaries (`quoteForm.images/mascot/automation/timing/serviceNames`). Submits to `/api/quote` (Resend + attachments, ≤4.4 MB total — the Vercel body ceiling): the route narrows every key to its catalogue, validates per service (`service-sections.ts` — `missingServiceFields`, mirrors the wizard gating; a payload without `service` is treated as the video form), and builds the Bulgarian email from `serviceEmailSections` + the shared contact / notes sections (subject `SERVICE_EMAIL_TITLE[service] - name`; markup in `email-template.ts`). Tests: `route.test.ts`, `quote-form-section.test.tsx` (jsdom polyfills in `src/test/setup.ts`) |
| `src/lib/server/form-guards.ts`, `src/lib/dates.ts`, `src/lib/found-us.ts`, `src/lib/social-links.ts` | Shared: request hygiene for both form APIs (clean/rate-limit/escape/EMAIL_RE), DD-MM-YYYY date display, the "how did you find us" key catalogue, social profile links (UI + JSON-LD) |
| `src/lib/youtube-embeds.ts` + `src/lib/portfolio-highlights.ts` | Portfolio clips per category, **newest first** in each array; embed as `/embed/ID` (never `/shorts/`). `portfolio-highlights.ts` maps category keys ↔ clip lists (`embedsForCategory`), exposes `youtubeThumbnailUrl` (`i.ytimg.com`, allowed in `next.config.ts`) and `portfolioHighlights()` (top 5 per category — currently unused) |
| `src/lib/partners.ts` + `partner-logo.tsx` | Partner list (logo filenames ↔ site URL) and the light/dark logo swap |
| `src/lib/projects.ts` + `src/components/projects-section.tsx` + `src/components/projects/` | Client case studies — the listing is **„Проекти“ in Bulgarian and "Case Studies" in English** (`header.projects`: the nav, the dock, the footer, the breadcrumbs, and the page's `title1/2`, `metaTitle`, `explore`, `viewAll`, `viewProject`, the showcase eyebrow / rail label and `detail.backToProjects` follow; the route stays `/projects`): identity data + numbers (category, style, YouTube id or `null` placeholder — or `clip`, a Bunny Stream video, for a project whose film is not on YouTube (MindGuard): the cards' poster + muted background player and the showcase frame read it in `videoId`'s place; `since`, platforms/views) in the lib; all copy under `projects.items[id]` in the dictionaries. Home shows `projects-showcase.tsx` — the scroll-driven "case studies journey" (ported from the Claude Design reference; line-up + order = `SHOWCASE_PROJECT_KEYS`, currently the five brands with a bespoke scene): a **transparent** sticky stage (the page background shows through the intro and between scenes) on a timeline in viewport units (`INTRO` 0.55 + `SPAN` 0.9 per project, ending with the last project's hold (`HOLD`·`SPAN`) so the stage scrolls straight into the next section with the last scene still framed — no empty tail; a scene sits framed for `HOLD` 0.72 of its span — about half a viewport of scroll, a long calm stay — and hands over in the rest, a fifth of a viewport, so a change is quick; scroll progress through `useScrollEased` — raw while Lenis glides the wheel, a stiff `useSpring` under native scrolling so canvases/players don't trail it — every style derived via `useTransform`, no rAF loop; the big soft glows in the worlds are radial gradients, never blur filters). Intro = cinema headline (`projects.showcase.*`, the section `h2`, theme tokens, `text-9xl` on desktop) that scales/blurs away, then the first scene opens from a mid-height slit via `clip-path: inset()` (`CURTAIN_START`→`INTRO`, eased in and out so it grows out of the line gently) — no curtain elements, so the page background stays visible; under the headline a **faint cinema**: a soft pool of light, a full-width letterbox band across the middle of the stage (hairline edges, a whisper of tint, widening a little through the intro) and a streak of light along its centre line that wakes once the headline is mostly gone (u 0.26 → 0.38) — the slit opens on that same line, so the picture grows out of the letterbox; all of it in theme tokens (`foreground/…`), fading as the picture grows past it; each scene holds for `HOLD` (slow 4% push-in) then hands over with the next of `TRANSITIONS` (`flow` diagonal wipe — the slow one, it starts 0.1 before the hold ends — → `push` → `pushX` → `zoomOut`, cycling; the outgoing scene never shrinks below scale 1 before the incoming is opaque); `sceneFrame()` is the pure geometry, and any frame at opacity ≤ 0.001 is also `visibility: hidden`. Each scene flips `inert` from its **own** local time (`parked` = t < −0.1 or past `PARKED_AFTER`, 40% into its hand-over), so an outgoing scene stays clickable until the next one covers it. Each scene = a **brand world** from `projects/showcase-scenes.tsx` (`sceneVisualFor`: `tone` light/dark, solid `background`, `layout` for the copy block, `morph` target, `Visual`, and an optional `className` — a world's geometry as CSS variables (OSMO's `--osmo-*`, Emblema's `--emb-*`, Plasico's `--pl-*`), all of them together on the **scenes' wrapper** (`SCENE_VARS`), so a scene's visual, its copy block and the morph shape read them alike) + the shared copy block drawn by the showcase — `BrandMark` per the scene's `mark` — `logo` (partner logo: white-ink variant on dark worlds, dark-ink one on a white pill otherwise, name text as fallback; `size="xl"` = `h-24` / `h-28` on phones / tablets and sized by the **viewport height** on desktop — `clamp(9rem, 22vh, 13rem)` / `clamp(10rem, 24vh, 14rem)` / `clamp(11rem, 26vh, 18rem)` at lg / xl / 2xl, so a 15" laptop (864px tall) gets a ~14rem mark and a tall monitor the full 18rem, and never wider than **30vw** from `lg` — the room between the two towers, whatever the height gives it (a px cap let it overlap them on a zoomed laptop) — on the `center`/`center-bottom` layouts — the Emblema logo **leads its title**, up top of a block centred at 49% from 2xl —, `"lg"` = `h-24` / `h-28` / `h-36` on `left-column`/`top-left-wide` (`h-16` / `h-20` on phones — MindGuard's mark two sizes up there), `"md"` elsewhere; the `sizes` hint follows the size, so the bigger renders are not served the 200px candidate; every scene mark loads **eagerly** — a parked scene is `visibility: hidden` and a lazy image in it only starts once the scene shows), `logo-large` (a dark-ink-only file rendered as a white silhouette via `brightness-0 invert`), `wordmark` (giant light-weight uppercase client name), `custom` (a scene-provided `Mark` component — unused today) or `none` (Boleron, Plasico and OSMO place their own marks; Mindguard leads its copy with the `logo`), headline, `items[id].highlight` — or the scene's own `Detail` block in its place (MindGuard: a small teal „Показано пред“ label over the list of where the film was shown, `showcase.scenes.mindguard.shownTo` — centred with the column from lg, a teal dot before each line, left-aligned in the compact phone block) —, **highlight tags** (`ProjectTagChips variant="highlights"` → *only* `items[id].tags`, in dictionary order — write the category into the tags where wanted, e.g. Boleron ends with "Software ad", Emblema = "Construction · Cinema ad"; **parked for now** — the block is commented out in the showcase, the tags still show on `/projects`) and CTA; the headline wears each world's own **display face** (`SceneVisual.headline`, classes on the `h3` — from the one map `PROJECT_DISPLAY_FONT` in `lib/project-fonts.ts`, which the project's own page wears too — all but Emblema's and MindGuard's, see the stories): Boleron **Montserrat** bold (corporate), Emblema **Playfair Display** italic, sentence case (the reference animation's Emblema face), MindGuard **Unbounded** (big and strong), Plasico **Exo 2** uppercase (tech), OSMO **Manrope** semibold (the face of the client's case study, its page's body too) — all self-hosted with Cyrillic via `next/font/google` in `layout.tsx` (`--font-montserrat/-playfair/-unbounded/-exo/-manrope`); a scene's `headline` may carry its own `text-*` ramp and width (Emblema's runs two sizes under the layout's and is capped at `28ch`, so „Емоцията на дома, разказана кинематографично“ — the client's line — breaks into two balanced lines under the big logo), and `highlight: false` drops the line under the headline (Emblema shows logo + headline + CTA only) — on a calm parallax layer (depth 0.25, so the copy drifts ≤6vh over the long hold and top-anchored blocks (`top-[max(9rem,18vh)]` below `lg`) stay clear of the header) placed per `layout` (`bottom-left` default; Boleron `boleron` = 50vw wide tucked toward the bottom-left with a navbar-sized margin (a compact block — `2xl`/`sm` type, dense chips — a rem under the word on phones, `--bol-top + --bol-word + 1rem`, with Roni below it); Emblema `center-bottom` (vertically centred from 2xl); Plasico `top-left-wide` = 28–30vw, vertically centred on the left (`52vh`, the copy layer drifts up a little during the hold), `7xl` headline only from 2xl; Mindguard `left-column` = the left 40% (`3xl` headline on phones, `4xl` from sm), logo + copy **left-aligned** in the column **under the header lane** (the mark file carries 5.3% of transparent padding on its left — the layout's `mark` row class and the story hero pull it back by 0.155 × the mark's height, so its ink lines up with the copy) (`lg:top-[5.5rem]`, so the taller block — `h-36` logo, three-line headline, the shown-to list, CTA — never rises into the pill during the hold's drift; `4xl`/`h-24` on laptops; below lg a compact block at the bottom under the tablet); OSMO `in-circle` = centred on the green copy circle (32vw, `5xl`→`7xl` headline; below lg a compact block — `2xl`/`sm` type (`3xl`/`base` from sm), `gap-2`, dense chips — inside the top of the small disc, under the white mark; its highlight is one short line that does not repeat the headline — „Всеки продукт в средата, за която е създаден - изцяло с AI.“); type ramps in `HEADLINE_SIZE` / `HIGHLIGHT_SIZE` per layout). The CTA is `ui/button-with-icon.tsx` (`ButtonWithIcon`: shadcn `Button` rendered as a `next/link` — or a plain `<button>` with `onClick` — sliding arrow disc on hover (a **named** group, `group/cta`, so a hovered card around the pill does not move its disc); the disc always carries the brand red→violet gradient (the `md` disc is `size-9`, a touch smaller than the pill's inner height and concentric in it — 6px from every edge — the `sm` / `lg` ones fill theirs), `surface` picks the pill: `dark` / `light` / `auto` (dark in the light theme, white in the dark one), `"lg"` is the service pages' hero CTA, `"sm"` the header's; `glow` wraps the pill in a soft red → violet halo — the header's). It is the **site's main CTA** everywhere („Поискай оферта“ / "Request a quote"): header (`auto`), hero (`auto`), service cards (`dark`, button mode), mobile sheet (`auto`, full width), the Emblema story's CTA band (`light`). A scene may ship an `Overlay` the showcase renders *above* the morph shape with the same frame transform (Plasico's big logo, so the green bar runs behind it). No "view all" button anywhere around the stage (the nav carries `/projects`). Light worlds flip to dark text, dark CTA pill and `TAG_CHIP_LIGHT_CLASS`. `MorphOverlay` is the one pill shape that travels between worlds, interpolating each scene's `morph` target during the hand-over (nothing on Boleron/Emblema → Mindguard hairline, hidden while that scene animates its own wave line (`ownsShape`; targets carry `xMobile`/`yMobile` for the sub-lg layouts — `yMobile` a stage fraction or a CSS length (a scene variable; the overlay's `top` is then a `calc()` mix of the two targets) — and are pre-scaled by the 1.04 hold push-in) → Plasico bottom bar → OSMO copy circle at **alpha 0**, no blend — the scene's own circle takes over). Worlds: Boleron (the client's cyan→blue→violet gradient with soft organic blobs + big arcs on two parallax depths — soft ellipses only, no capsules (they read as chips), the bottom-right one wide and nearly level behind Roni —, the word "Boleron" as **live text** behind Roni — up top on phones, leading the stack (`22vw`, at `--bol-top`, never higher than 8rem — its parallax layer, depth 0.55, lifts it about 5vh by the snap stop and it must stay under the bar), on desktop flush left right above the copy block (`lg:left-[max(2rem,4vw)]`, bottom-anchored `8vh + 18rem` (`20rem` from 2xl) so it clears the block, `14vw` → `15vw` → `16vw` by breakpoint — small enough to stay under the floating header on short laptop screens) — set in **Montserrat ExtraBold** — the face the reference animation uses, self-hosted via `next/font/google` as `--font-montserrat` in `layout.tsx` (700 + 800; Circe Rounded, the client's real face, is commercial and not in the repo — the rounded open faces closest to it, Nunito Black and Fredoka, were tried and read worse) — Roni as a 72-frame scroll-scrubbed `FrameSequence` from `public/projects/boleron/roni/` that finishes early in the hold (t ≈ 0.15) so the last pose is held for most of the stay, then sinks and fades out as the scene hands over (from the flow wipe's start on desktop, from the early phone hand-over on phones — faster there, gone within 0.16) — bottom-right on desktop, on phones **at the foot of a centred stack** — the word, the copy block a rem under it, Roni two rem under that (the Boleron scene's variables: `--bol-word` 22vw, `--bol-block` ≈ 11.5rem / 13.5rem from sm, `--bol-roni` = `min(36vh, the room left down to the dock)` (`44vh` from sm), `--bol-top` = `max(8rem, (100svh − word − block − Roni − 2rem) / 2)`), his legs fading out toward the dock), Emblema (cream, gold rings + horizon, gold glows/dust, the two buildings as a split 72-frame sequence (`--split-gap` 18% on desktop, **−10%** on phones — the halves pulled toward each other, so the towers stand close — where they sit **1.5rem under the copy block's CTA**, the two as **one stack centred on the screen**: the scene's variables (`--emb-block` ≈ 14rem — mark `h-24`, headline, CTA —, `--emb-towers` = the full-width towers' height, 55.76vw for the 1284:716 frame; 16.5rem / 44.61vw at 80vw from sm) give `--emb-top` = `max(6rem, (100svh − block − towers − 1.5rem) / 2)` — the block's top, never up into the header — and the towers stand at `--emb-top + --emb-block + 1.5rem`; the labels under each tower are desktop-only) from `public/projects/emblema/buildings/` with labels from `projects.showcase.scenes.emblema.buildings`), Mindguard (navy, dot grid + dashed orbit + teal rings + wave line at 86% (42% below lg); 4:6 columns — white mark + copy centred in the left 40%, the tablet frame (`52vw`, scales with the viewport) pinned bottom-right; **below lg** the tablet sits up top under the header (`84vw`, width-capped by the viewport height — `(100svh − 34rem) × 1.5` — so it never meets the copy) with the glow behind it, and the copy block is a compact one anchored at the bottom (`bottom-28`, `2xl`/`sm` type, the `lg` mark at `h-11`, dense chips — `DENSE_CHIPS` in the showcase) — plus the same mark ghosted, big and floating (`animate-float`) along the bottom; `videoId` is a **stand-in** clip until the Mindguard explainer is on YouTube; the partner mark `mindguard_logo_dark.png` is a **white-ink** file — `Partner.invertOnLight` inverts it on light surfaces in the marquee / case study / logo circle), Plasico (white, pinstripes + green rings, the **client's logo, big** (`PLASICO_LOGO`, the green „plasico · IT superstore“ file, 497 × 128 — it replaced the giant text name) bottom-left on desktop (`min(36vw, 560px)` wide, `3vh` up, a green drop shadow); **below lg it leads a stack** — logo, copy, video — centred on the screen: the scene's variables (`--pl-name` = the logo's height, 15.45vw at 60vw wide / 12.36vw at 48vw from sm; `--pl-block` ≈ 12rem / 13.5rem; `--pl-video` = the 16:9 frame's height, 49.5vw at 88vw / 45vw at 80vw) give `--pl-top` = `max(6rem, (100svh − name − block − video − 2.5rem) / 2)`: the logo at `--pl-top`, the copy block 1.5rem under it, the frame a rem under that; the morph bar rides the logo there (`MorphTarget.yMobile` may be a **CSS length**, here `--pl-name-cy` — the logo's centre less 2.8vh, because the logo rides a parallax layer (depth 0.3, −35vh × depth × t) and has drifted that far up by the time the scene is framed at t 0.27 while the bar has not; and below lg the bar is only 1.2 × the logo's height (`MorphTarget.hMobile`, a CSS length like `yMobile`; `h` is a fraction + svh + px otherwise), so it never reaches the headline) —, copy (left, ≤30vw; on phones shorter than 700px its highlight line is hidden — `max-lg:[@media(max-height:700px)]:hidden` — so name + frame still fit above the dock) · the one 16:9 frame (54–56vw, 52vw from 2xl, vertically centred at 47vh and reaching the right margin — the tall clip is gone; 88vw / 80vw at the foot of the stack below lg) playing the YouTube clip (the Bunny Stream film `PLASICO_WIDE_VIDEO` is commented out in the scene, ready to swap back); its morph bar uses the very muted secondary green — Bunny (`iframe.mediadelivery.net`) sources build via `src/lib/bunny-stream.ts` (`bunny(id)` for a clip in the site's one library 750681, `bunnyPlayerEmbedSrc` / `bunnyBackgroundEmbedSrc`, and `bunnyThumbnailUrl` — the poster on the library's pull zone `vz-49fa6283-9c1.b-cdn.net`, which only serves requests with the site or localhost as referrer, so posters are plain `<img>`s (`unoptimized`, `referrerPolicy="origin"`), never the image optimizer; the share links the client sends are `player.mediadelivery.net/play/750681/<guid>` and their titles / dimensions are on that page's `og:` meta and the `/embed` page's JSON); `null` shows the branded placeholder), OSMO (white; **one group centred on the screen** from `lg`: the wordmark over the **4:5** frame on the left, a 2vw gap, then the green copy circle (`min(46vw, 88vh)` wide, its centre at 56vh) — the geometry lives twice, on purpose in step: as the `--osmo-*` CSS variables on the scene root (`SceneVisual.className`: `--osmo-frame-h` = `min(62vh, 45vw)` (the frame is sized by its height, about 26vw on a 16:9 screen; the 9:16 YouTube clip cover-fits it, losing 15% top and bottom instead of the old 4:3 frame's 58%), `--osmo-d`, `--osmo-left` = the frame's left edge = `(100vw − frame width − d − 2vw) / 2`, `--osmo-cx` = the circle's centre — the visual, the copy block (`in-circle`, 32vw centred on `--osmo-cx`) and the wordmark all read them) and as `osmoCircleDesktop(vw, vh)` in `showcase-timeline.ts` in px for the journey's morph (`OSMO_CIRCLE` keeps the nominal 16:9 centre, x 0.65, for the scene's own morph target); a gap rather than an overlap because the journey's canvas draws the circle *over* the stage at the hand-off and would cover a frame under it. The wordmark rides the frame's parallax layer so it always sits 1.5rem above the frame, as tall as the room left under the floating header — `clamp(6rem, 94vh − frame height − 8rem, 11rem)`, which lands its top 6.5rem down whatever the screen: 6rem on a 650px-tall laptop, 11rem on a big monitor; below lg the disc is small — `min(88vw, 52vh)`, centred, its top edge just under the header (`max(5.75rem, 10vh)`) — with the white-ink mark (`h-20` / `h-24`, `1.25rem` under the disc's top) and the compact copy block inside it under it (`+6.5rem` / `+8.75rem` from the disc's top — mark, three-line headline, the three-line highlight and the CTA end about 20px above the frame's top on an 844px phone), and the 4:5 frame — drawn *after* the disc, its top 4% of the disc's diameter up over the disc's bottom cap (under the copy's CTA), as tall as the room down to the mobile dock allows (`h-[calc(100svh − …)]`, width from the aspect ratio, never wider than 92vw — `max-h-[115vw]`); `osmoCircleMobile()` in `showcase-timeline.ts` is the same disc geometry in px for the journey's morph — keep the two in step) — layered radial highlight, dot pattern, concentric hairlines, deep shadow — with the copy inside in white (`tone: dark`, layout `in-circle`); the left of the group = wordmark up top, 4:5 frame at the bottom. The morph shape arrives on the circle with **alpha 0** (it sits above the scenes, so it must never cover the copy); from `OSMO_OUTRO_START` (`HOLD` − 0.2, well past `SCENE_FRAMED`) the copy fades over `OSMO_COPY_FADE` (0.08) and leaves the tab order, then, from `OSMO_HANDOFF_START` (`HOLD` − 0.12; the stage's own fade runs over the last 0.08 of the hold), the home journey's fixed canvas draws the copy circle itself on top (`journeyMorph`, see `scroll-journey.tsx` — same spot and size, following the layer's parallax + the hold push-in via `osmoCircleLayer`) so the white world can dissolve underneath on the **raw** scroll progress (`endFade`, no spring lag), the stage is transparent the instant it unpins and the circle simply slides on into the stats' ring — nothing swells over the screen any more (the old ×8 `OsmoOverlay` is gone). The timeline numbers both sides line up on (`SHOWCASE_INTRO/SPAN/HOLD/UNITS`, `SHOWCASE_UNIT_VH` — one unit is (UNITS − 1)/UNITS of a viewport, since the section is UNITS × 100svh with a 100svh stage —, the OSMO outro times, `OSMO_HANDOFF_VH`) live in the plain module `projects/showcase-timeline.ts`); anything else gets `FallbackVisual` (accent gradient + clip). Primitives live in `projects/showcase-primitives.tsx` (`ParallaxLayer` depth/dx/rotate/scale vs. scene-local time, `Reveal` staggers, `Sparks` → `animate-showcase-rise`, `FrameSequence` (`split` draws the left / right halves on two canvases pulled apart by the caller's `--split-gap`) — frames (re-encoded to the canvas backing size, ≈2.2 MB per sequence) load coarse-to-fine (every 8th first, six in flight) once the section is within 800px (`useInView`), each decoded before it counts as ready, nearest ready frame drawn meanwhile; on a **coarse pointer** every other frame is loaded and kept as a decoded `ImageBitmap` at the size it shows at (1.5 × the CSS size, at most 720px wide — 25–60 MB a sequence, retained while the sequence is mounted): a phone drops decoded images from its cache between frames and re-decoded one per drawn frame, which was the stutter (a trace showed the decode gone from ~1.5 s to nil across the stage); a fine pointer keeps the plain decoded `<img>`s (all 72). Phones also skip the towers' `drop-shadow` (a filter on a canvas that repaints every frame) and the intro headline's blur, and the glass bars' backdrop blur drops to 12px there —, `EmbedCover` cover-fit background player (any `src` — YouTube or Bunny) for a box aspect, `ProjectEmbedCover` for a project's YouTube clip); `animate-showcase-breathe` is the slow glow/building pulse. Players are mounted for the framed scene, the previous one while it is still leaving, and the next one from 10% into the hold (`mounted` set) — and only while the stage is on screen (`useInView(sectionRef)`), so nothing keeps playing under the rest of the page. `Project.showcaseClip` (a Bunny clip — MindGuard's UI/UX film plays on its tablet, Plasico's frame plays its "Back to Work 4K" ad) or `Project.showcaseVideoId` (a YouTube stand-in, unused today) is footage for the stage only and is never shown on the case study / cards; `ProjectEmbedCover` picks the source in that order, then `videoId`, then `clip`. Stage furniture: the right-hand rail (`nav` + `ul` of jump buttons with `aria-current`, counter above — a jump lands on the scene fully framed, `SCENE_FRAMED`) as bare white marks in `mix-blend-difference` (no pill — they invert over light worlds); hidden (`visibility`) while transparent. Parked scenes are `inert`; every transparent frame is `visibility: hidden`; stage and scene roots use `overflow-clip` (never `overflow-hidden` — focus must not scroll them). Any scene `Overlay` sits above the morph (z +30) and fades exactly with the incoming scene's cover during its hand-over. Reduced-motion users get the plain card list; `/projects` lists all (quote CTA in the page header, category card on the left, floating scroll-revealed cards two-up and row-aligned on desktop - same in-frame standard as the showcase: the clip plays muted behind the copy via `ProjectBackdropMedia` once the card nears the viewport, logo circle + tag chips on top, headline + dominant "view project" pill at the bottom); `/projects/[slug]` is the case study: the generic layout — **no project uses it today** (iSupport, the last, was removed), it stays for the next client without a story — (the logo — through `ClientSite` too, linked to the client's site when the partner entry has an `href` —, chips + highlight tags, mission, stats, platforms, quote wizard; `Project.world` paints the client's ground behind it in both themes — e.g. MindGuard's `#F3F7F7` / `#0E1116` with the teal `#45A199` — though MindGuard now has its own story; down to the footer, which keeps the page ground; the same `STORY_CONTAINER` width and a big bare logo in the header panel) — or, for a project with a **long-form story**, its own page: `projects/project-story.tsx` is the registry (`PROJECT_STORIES`: emblema · osmo · boleron · plasico · mindguard — `ProjectCaseStudyView` renders the entry if there is one), the pages live in `projects/story/` on the shared kit `story/primitives.tsx` (`StoryShell` = site chrome + reading-progress hairline + breadcrumbs around the story block, with the story's CSS tokens (`--story-accent/-muted/-line/-rule`, even `--foreground` for a colour world) scoped to that block, **the story's own ground** — a solid in the client's colours set as `--story-ground` on the shell's `className` (light, plus the `dark:` variant) and painted by its `ground` node, which the story's tinted cells mix from instead of `--background` — running behind the story **and the quote wizard**, right down to the footer (the footer's `mt-20` becomes the article's `pb-20`, so the breathing room above it is on the ground); the wizard keeps the site's own tokens so its cards / fields follow the theme, and a colour world hands `wizardHeading` white ink tokens for its heading alone; only the footer sits on the normal page ground. **`wizard={false}` drops the quote wizard** after a story (MindGuard's page ends on its own CTA). **`theme` locks the whole block — story, breadcrumbs and wizard — to one theme whatever the visitor's** (`theme-light` / `theme-dark` on the article, the mechanism under the theme rules in §4): the client wants Boleron, Emblema and OSMO in their light colours and MindGuard in its dark ones — the `dark:` sets those stories carry are inert under the lock and stay for the day it is lifted; Plasico still follows the theme; the header and the footer around the block keep the site's theme. Every story page is laid out on `STORY_CONTAINER` — the usual 80rem on laptops, **80% of the screen** on anything wider (`min(calc(100% − 3rem), max(80vw, 76rem))`, never narrower than before), full width with padding on phones — with type a size up from `xl` (`Display` headings `xl:text-6xl`, leads / `BodyXL` `xl:text-2xl`, stat numerals `clamp(3rem, 6vw, 7rem)`, hero titles on the fluid **`HERO_TITLE`** scale — `long` for a whole-sentence title (Boleron, OSMO: about 2rem on a phone, 3.5rem on a 1366px laptop, 4.5rem at full HD), `short` for three or four words (Plasico), `light` for Emblema's uppercase lines — so no hero title spills past the fold on a short laptop) and `fadeUp` at 1s. Every hero leads with the **client's logo, big and bare** (`PartnerLogo`, `h-20`→`h-28`, OSMO `h-24`→`h-36`, MindGuard `h-[4.5rem]`→`h-24` as in its file; no card, no "case study · name" eyebrow — Boleron's dark-ink mark becomes a white silhouette via `brightness-0 invert`, Emblema's copper mark reads on both of its grounds — and from `lg` Emblema's stands **on the right** of its hero, big (`h-48`→`h-64`), the title on the left; **the logo itself links to the client's site** — nothing visible added: `ClientSite` (the kit) wraps it in the link (a new tab; the accessible name is „Към сайта“ / "Visit website", `projects.visitSite`, plus the domain, which the title carries too), the `href` from the partner entry (`PARTNERS[].href` — MindGuard's is mymindguard.ai; without one the logo stands alone), `className` the link's own box (MindGuard's padded mark pulled back in line); a small arrow line under the logo was tried and the client wanted only the logo; on top on phones); the **large letters wear the brand's display face** — `StoryShell display={PROJECT_DISPLAY_FONT[id]}` hands it to `Display` and the CTA title through `useStoryDisplay()` (it goes after the tone's family / weight / case and before the caller's `className`), and the hero `h1`s and the stories' `TITLE` consts take the same classes directly (after `font-heading`, before the leading); MindGuard's page has the type scale of its delivered case study instead (see its story); **Emblema's page opts out** — no `display`, its large letters stay the airy uppercase extralight (the client found the Playfair page busy; the face stays on its showcase scene), and they do not rise word by word either: `SoftTitle` (in the story) settles each line in softly — opacity, a slight blur (10px → 0) and a breath of scale (1.035, bottom-left origin — a transform, so a wrapping line never re-breaks mid-way) — and it is **pure CSS** (`soft-title-in` in `globals.css`), so a title is on the page the moment it paints and can never wait for hydration or a JS reveal (the client saw titles arriving late): the hero's lines play on load (`.soft-title-load`, 1.25s, staggered through `--soft-delay`), every other title runs on a **scroll-driven timeline** as it enters the viewport (`.soft-title-view`: `animation-timeline: view()`, `animation-range: cover 0% → cover 26%` — the reveal is done by the time the line is a quarter of the way up the screen; a browser without `animation-timeline` shows the plain title, and so does reduced motion); nothing JS-driven stands between the reader and those titles — the section heads are plain blocks, the film entries' heads too, and its `Split`s pass `leftReveal={false}` (the copy column still fades in after `COPY_DELAY`), the CTA title goes through `CtaBand titleNode`; the copper `INK` sits on each line span, because a filter or an opacity on a child of a `bg-clip-text` element leaves the clipped ink untouched —; `Section`, `Eyebrow`, `Words` / `Display` (headings rising word by word — the words start a little *before* the heading enters the viewport (`WORDS_VIEWPORT`, a +6% bottom margin, where the other reveals wait until 8% in) and run 0.65s with a 0.025s step, the hero titles' `base` 0.05, so they are in place by the time the reader reaches them; `tone="light"` for Emblema's airy light weight), `Lead` / `Body` / `BodyXL`, `Split` (sticky heading column, `ratio="1/2"` — the heading column first, the copy column `COPY_DELAY` 0.25s after it via `fadeUpAfter(delay)`, the rule for every heading + copy pair: **the title is always read first**; `leftReveal={false}` leaves the heading column still for a story whose titles reveal themselves (Emblema); the film entries and OSMO's product sections stagger their own copy the same way), `FactsStrip`, `StatGrid`, `MetaList` (label / value pairs a size up, a value may be a node — Boleron's bTV mark on a white pill), `Collage` (skewed image panels that open on hover, stacked on phones), `ClipCollage` (the same skewed row for **clips**: the Bunny poster in each panel (`bunnyThumbnailUrl`, a plain img), the hovered one opening up and playing its clip muted through `EmbedCover`, a click opening the full player in `ClipLightbox` — the shadcn / base-ui `Dialog` from `ui/dialog.tsx`, its first use: Escape, the backdrop and the X close it, the close button sits first in the DOM so the initial focus is the page's, not the player iframe's; below md a two-up grid of posters that open the lightbox), `MediaFrame` (self-hosted mp4 with poster + native controls / YouTube / Bunny player via `bunnyPlayerEmbedSrc` / branded placeholder), `Reveal`, `frameIn` (a frame or card rising and settling in), `Drift` (a media block drifting a few px against the scroll while it crosses the viewport — the alternating sections' media; still under reduced motion), and `CtaBand` — the band **every case study ends on** (the stories and the generic layout alike), right above the wizard (`titleNode` swaps the string / `Words` title for one the story renders itself — Emblema's CSS lines): a banner with the title running wide on the left (its own `max-w-[22ch]` cap on the `h2` itself — a cap on the wrapper folded it into a narrow column) and the quote pill (`#quote`, the wizard below) + a contact pill (`contactProcessPath`) beside it from `lg`, everything stacked on phones (`center` stacks it all centred); `tone` light / dark / page picks the pill surfaces, the container look and **each project's dressing** come from `className` + decoration `children` (absolutely positioned, rendered first, clipped to the rounded band, the copy kept above them): every card **theme-adaptive with a soft brand-coloured glow** (`shadow-[…-30px_rgba(brand,0.5)]`): OSMO's white card — deep green `#0F2418` in the dark theme — in its leaf-shaped green frame with two corner lights (see the story), Boleron's white card — deep blue-black `#10173A` in the dark theme — with the brand gradient as a hairline along the top and a blue light, Plasico's green → lime card with its pinstripes and a white light (the brand gradient a shade darker — `BAND` in its story — in both themes), Emblema's centred copper band with the faint `Arches`, MindGuard's card in the platform's teal light, dot grid and ring on its charcoal (dark) or its pale teal (light), and the generic layout's card in a `Project.world` (dark ground + `accent`; a plain card surface without one); a theme-adaptive card uses `tone="page"` — auto pills, the outline pill in `currentColor` — with its ink flipped by `dark:`; `animate="rise"` makes the card rise and settle with its copy and pills staggering in, `words` brings the title in word by word in the banner layout (Boleron); every reveal runs **once, on the way down** (`VIEWPORT`), never backwards). Each story is its own world: **Emblema** (`emblema-story.tsx`: the whole page on Emblema's own ground, **locked light** (`theme="light"`) — the showcase's cream `#F3EFE8` with a soft white light up top whatever the visitor's theme (the warm black `#17130F` + copper light set for the dark theme stays in the file, inert), no bands — and the type carries the **logo's copper** (`COPPER` light / mid / deep: headings in the gradient via `INK` = `bg-clip-text`, eyebrows / numerals / hairlines / step titles in the mid tone); the hero is **copy alone** — the uppercase title (`SoftTitle`, on load, `base` 0.15) and the line (`fadeUpAfter(0.6)`) on the left, the logo big on the right from `lg` (`lg:grid-cols-[1.15fr_0.85fr]`, the logo column `lg:order-2`; on phones the logo comes first), then the meta strip across both columns in five columns from `lg` (a field listing several things, the two projects, reads as dashed lines — `value` is a `string[]` there); no film up there any more (the client wanted the clean version — the arch with the muted film and its logo card is gone); the films (`FilmEntry`) each rise in with `frameIn`, the number / title / project first and the text + tags a beat after — the wide first film across the page with its copy under it, each **vertical film in its arch beside its copy** (`lg:grid-cols-2`, the copy capped at `52ch`), the sides alternating (`side`: the first tall one on the left, the next on the right; on phones the arch comes first, then the copy); the copy is the client-approved case-study text from the delivered `EmblemaCaseStudy.jsx` (hero, client, three films, challenge, approach, process, hard parts, result, CTA — the CTA pills keep the site's wording), film frames are `.arch-wide` / `.arch` (`FilmMedia` takes a `StoryClip` — YouTube or Bunny), `ArchMark` draws itself at the key sections, stats and the CTA band sit on a faint copper tint; the films are the three Bunny clips in `Project.story.films` (`clip` + `orientation`: "Eria Video 1 – Movie Style" wide, then the two vertical social cuts in the client's order — "Eria Video 3" (the cyclist, „Повече място за живот“) under the second item, "Eria Video 2" (the family in the park) under the third — NOTE(content): the third item's copy still describes the District Living film while both vertical clips are ERIA social cuts), the hero cover stays the project's YouTube thumbnail), **OSMO** (`osmo-story.tsx`: **in the dress of the client's case study** (`OSMO-Case-Study.html`) — **Manrope** for everything (`StoryShell font` for the body at 400; the large letters `PROJECT_DISPLAY_FONT.osmo`, Manrope 600 with `tracking-[-0.02em]` — the showcase headline wears it too), the page **one gradient from white down into OSMO green** (`GROUND`, the file's: white for the first 38%, `#EAF4EC` at 58% under the films, `#5FB27A` at 80% — where the results start — and `#108C3C` at the bottom, drawn over the story block; the article's ground is that green, so the wizard sits on it with its heading white), **locked light** (`theme="light"`), OSMO green `#108C3C` for the accents; the fan-of-samples hero clip (a 9:16 file) **bare on the white, no frame** — at its own frame, `max-h-[min(82vh,780px)]` in a 520px column, multiplied so its white ground is the page's (the leaf `GreenFrame` around it is gone; `GreenAura` — a radial light, never a blur filter, plus two `Sparks` bands — `LEAF` and `EDGE_CARD` still dress the CTA card), a 5-column facts strip, challenge / solution, the 4-panel collage, four product films — the Bunny clips in `Project.story.clips` (`lazur` · `singleCoat` · `uv` · `decking`, in each one's real frame: 4:5, 9:16, 4:5, 4:5 — the player letterboxes anything else; the self-hosted cuts are gone, only the hero clip stays local) with format / length / channel, then the results **in white on the green** (a white/25 rule above) and, on the green, the client's quote card and the CTA card — the leaf shape with the green gradient edge on the card itself (`EDGE_CARD`: a transparent 3px border, the card colour on the padding box and the gradient on the border box, so the edge rises with the card), green lights in two corners, and the aura's pale (near-white) light + particles behind it), **Boleron** (`boleron-story.tsx`: the whole story block is the showcase scene's gradient, **left → right** (cyan → blue → violet at 90°, a soft white light and a deep-blue settle toward the bottom) with white type (`ground`) — the whole block **locked light** (`theme="light"`) — the gradient, the white CTA card and a light wizard whatever the visitor's theme (the client wants the page in its key-visual colours; a deeper cut of the gradient for the dark theme was tried and dropped), the set living as `--bol-*` variables on the shell (`GROUND_VARS`) that the ground reads; the hero sits right at the top (`pt-0`, the title left and Roni right, **centred against each other** from `lg`; Roni's composite is sized to the first screen — `w-[min(100%, (100svh − 14rem) × 9/16)]`, the header, the breadcrumbs and a margin taken off the viewport height, never over 520px — so all of him, legs included, is on screen: at 9:16 of the column's width his legs used to run below the fold) and the wizard's heading is white on the colour (`wizardHeading`) while its cards follow the theme; Roni stands in the hero as a **composited clip** — `roni-alpha.mp4` is colour + matte stacked, un-premultiplied per video frame onto a canvas (`requestVideoFrameCallback`, only while on screen) so he floats over the gradient and tilts toward the pointer; phones / reduced motion get the plain `roni.mp4` in a frame (`RoniClip`) — then the facts and, in this order: the **bTV spot first** (`clips.tv` — „Boleron TV 3“ on Bunny, a 960 × 1080 file — in an 8:9 player (`max-w-md`) with the bTV mark on a white badge in its corner, the copy taking the bigger column (`Feature ratio="copy"` — 1.3 : 0.9, only for `side="right"`, where the copy is the first column) (`TV_BADGE`, `btv.png`); the self-hosted 8:9 cut and its poster are gone; its `Feature` is `align="start"` — the frame sits at the top beside the long copy — and the channel in its `MetaList` is the same mark on a white pill, both pills with room around the mark — `px-4`), the **numbers right after it** (three of them — videos, views, the one hero; the posts count is gone — as a `StatGrid` on a glass panel, `GLASS`, with the closing line beside the title), and a run of **alternating `Feature` sections** — a short title + description (+ note / meta) on one side (`max-w-[58ch]`, the grid 1.05 : 0.95), the media on the other (`side`, `align` centre or start), the copy staggering in, the media `frameIn` then `Drift`ing with the scroll; copy first on phones: the challenge with Roni's scenes as a **`Mosaic`** (one tall panel + four squares with their labels, the old skewed collage is gone), the character beside one of his films (`clips.character` — the 16:9 „Гражданска 2“ on Bunny, the same clip as the `liability2` ad — in a `MediaFrame`; `RoniClip`, his plain clip in a frame, is only the hero's phone / reduced-motion fallback now), then **the eight product ads** (`AD_KEYS` — summer · casco4 · liability3 · property · travel · liabilityApp · casco3 · liability2 — the client's 16:9 Bunny clips in `Project.story.clips`) as a `ClipCollage` spanning the page (the section's `inner` is widened, the heading keeps `STORY_CONTAINER`), titles + notes from `stories.boleron.ads.items`, the YouTube section playing `clips.youtube` (the 4K „Гражданска Отговорност 3“ on Bunny, the same clip as the `liability3` ad — the YouTube pre-roll embed is gone) and the social phones playing the client's three **vertical cuts** on Bunny, `clips.shorts` / `facebook` / `tiktok` (Автокаско 3 · Автокаско 4 · Гражданска отговорност, 9:16 players in the phone frames, the middle one lifted; three across from `sm`, **stacked one under the other on phones**, each 18rem wide and centred) — every clip from `Project.story.clips` (`null` = the branded placeholder) — and the CTA card (white / deep navy `#0F1638`, `animate="rise"` + `words`, an eyebrow and a lead, a fine dot pattern in `currentColor`, two arcs top-right, the brand gradient as a hairline along the top, a blue / cyan light; the eyebrow's `--story-accent` is re-set to blue / cyan inside the card), **Plasico** (`plasico-story.tsx`: white with a hint of green `#F3FAF1` / deep green `#0B1810`, Plasico green + pinstripes — told in **big, short titles over large type** (they are what gets read): the hero in two columns (1 : 1.1 from `lg`): **the client's mark on top** (`h-16`→`h-24`, the link to its site), the title under it — a step under the short scale on desktop, `clamp(2.5rem, 1rem + 2.4vw, 4.5rem)`, the uppercase Exo 2 is loud enough — and the lead, with the **"Back to Work" film playing muted on the right** — `EmbedCover` in a `rounded-[2rem]` 16:9 frame with a green radial glow, rising in with `frameIn` (under the copy on phones; the mark used to sit beside the film under a page-wide title), the brief ("content on a new level"), the three ads — the two wide ones each an eyebrow + title **across the page**, the two lines and the format note under it at **60% of the width** from `lg`, then the full-width 16:9 frame that rises and settles in (`frameIn`) (the two-column head — title left, text right — is gone; a views stat under each ad — a small title, an icon and „600k+ гледания“ — was added and removed again the same day); the vertical **third** ad instead sits **beside** its copy (`ADS[i].beside`: title + text left, the 9:16 frame right) — the full-bleed green "how we work" band with its three numbered points, and the green CTA card — both on `BAND`, the brand gradient a shade darker (`#1A8E25` → `#52AC28`; the client found the plain green → lime too bright at the foot of the page); the clips come from `Project.story.clips`, all on Bunny Stream, mapped to the titles the client's way (`ADS`): `office` = the office spot "v1 3rd voice 2nd music" (16:9) under „Геймърски детайли. Послание към офиса.“, `backToWork` = "Back to Work 4K" (16:9 — the hero's muted film too) under „По-закачливо. По-точно.“, `backToSchool` = "Back to School" (the vertical cut, the 9:16 frame beside its copy) under „От плажа до гейминг стаята“; the "Hot Summer Sale" spot is off the page). **MindGuard** (`mindguard-story.tsx`: **the client's own case study, as delivered** (`MindGuard-Case-Study.html`) — the same sections, copy and look, on the platform's colours from mymindguard.ai: the charcoal `#0E1116` sinking into the teal `#35827C` down the page (`GROUND`, the delivered page's gradient — charcoal for the first 38%, `#12302F` at 58%, `#2A6F69` at 80%, the teal at the bottom — drawn over the story block; the article's ground is that teal, so the wizard sits on it with its heading white), pale ink `#F2F4F6`, the teal accent `#45A199`, **locked dark** (`theme="dark"`), and set in **the platform's own faces**: Space Grotesk for the titles (`HEADING` — `font-heading` is inlined to Exo 2 by the theme, so the class sets the family itself), DM Sans for the body (`StoryShell font`); neither has Cyrillic, so Manrope follows them in the stack and stands in for the Bulgarian glyphs, as the delivered page does (its own fallbacks). The page: the hero as **two columns** (1.1 : 0.9 from `lg`, centred against each other) — the logo (the link to the client's site) right above the „Case study · MindGuard“ eyebrow, the h1 at 500 on a fluid clamp and the lede on the left, and on the right **the key visual's phone on its rock** (`public/projects/mindguard/story/hero-phone.webp` — the right part of the file's picture, 1400 × 1591), sized by its column and never over 640px, its edges feathered into the ground, so it scales with the screen; on phones it sits **between the lede and the facts**, full width up to 520px — the logo stays on top (the full-width key visual with the copy laid over its left two thirds was tried — the client wanted the phone responsive and to the right, and no gap between the logo and the title); the title reads „AI презентационни видеа за един от най‑обещаващите стартъпи в Швейцария“ (the "user interface and showcase" of the file's title dropped); a 5-column facts strip between full-width hairlines — Client · Industry · Product · Delivered · Partnership, the client's order; below `md` the client and the industry share the first row and the other three take a row each (`max-md:col-span-2`); the challenge (eyebrow + h2 left, the three paragraphs right, 1 : 2); three **`Solution`** sections — the copy (eyebrow, h2, paragraphs, a three-column meta list — two columns on phones, the third item, the long "live presentation", on a row of its own) beside the film in a `FRAME` (rounded-xl, a hairline, a deep shadow; 0.85 : 1.35, `flip` puts the frame on the left — the President's; the copy first on phones): the presentation to Klaus Schwab (`clips.pr`), the one to the President of Switzerland Guy Parmelin (`clips.president`), the 1+1 TV presentation (`clips.tv`) with the **TV segment** (`clips.broadcast`) across the page under it and its caption; the results **in white on the teal** (a white/25 rule above, three numerals with a small suffix — the third across the row on phones —, the closing line); the two testimonials in white cards (`Quote`: the eyebrow on the first only, the big quote mark, the quote in the heading face at 500, name + role); the CTA in a white card — the question (its second, user-interface sentence dropped at the client's ask) and a teal pill „Свържете се с нас“ to the contact page (no `CtaBand` here); then straight into the footer — **no quote wizard on this page** (`StoryShell wizard={false}`; the client first wanted only the services kept after the case study, then none of it). Copy in `stories.mindguard` (`hero` · `facts` · `challenge` · `solutions.schwab / parmelin / tv` (+ `caption`) · `results` · `testimonials` · `cta`): the Bulgarian verbatim from the file, the English our translation, the testimonials in English in both; `videoId` stays `null` (nothing on YouTube) and `Project.clip` = the PR film for the cards; the showcase scene keeps its Unbounded headline). Story copy lives in the dictionaries under `projects.stories.<id>`, each with its own shape (the OSMO / Boleron Bulgarian copy is the client-approved case-study text — Boleron's tightened since: connected sentences instead of „Целта беше ясна:“ lead-ins, „няколко концепции“ rather than a count, the speed-and-scale line dropped, plus a short line each for the YouTube / social sections and the CTA's eyebrow + lead; the English is a translation; Plasico's is ours, written from the client's account of the three ads). Slugs = `PROJECT_KEYS`, pre-rendered, in the sitemap |
| `src/components/` | Page sections (hero, quote wizard, reviews, faq, contact… — `process-section.tsx` draws a brand-gradient wave through its four (10rem) badges — on the home journey over the hold, elsewhere as the row scrolls in — a glowing bead riding the tip; below `lg` the stacked steps get a subtle **S-curve** (`CURVE_PATH`, a 100×1000 viewBox stretched to the column, half-strength brand colours) that draws as the column scrolls up the screen (`useScroll` on the column, `start 75%` → `end 82%`) with its own bead (the paths' lengths are measured once — a `getTotalLength` per frame was a forced SVG layout); the badges are 512px WebPs served `unoptimized` + `loading="eager"` because the dev optimizer route stalled on one of them and the `<img>` then waited forever on the coalesced request)) + `training/`, `services/`, `legal/` views |
| `src/components/ui/scroll-journey.tsx` | `ScrollJourney` + `JourneyScene` + `JourneyItem` — the home sections after the showcase (stats, reviews, training, contact, process, FAQ in `home-page.tsx`) as a scroll-driven journey where the sections stay transparent (the fixed page background never moves) and their **parts** travel: each section marks its heading block (`<JourneyItem kind="title">`) and its components (`<JourneyItem index from="left|right|bottom|top">` — stat cells, review columns, training cards, the contact form + details, process steps, FAQ questions) and the choreography on scroll is: previous title leaves ▸ previous components leave (staggered, up and back toward their side) ▸ next title rises in from below ▸ next components arrive from their sides (staggered) — the incoming title starts a little before the outgoing parts are all gone (`arriveStart` 0.44), so the two cross-fade rather than cut. `JourneyItem` is a plain wrapper outside a journey (e.g. `ContactSection` on `/contact`). Each scene is a sticky box (at least a viewport high, `pt-24` for the floating header, content vertically centred in the first viewport, plus **reading room** — `hold` of a viewport of bottom padding, default `HOLD` 0.2, so even a screen-high scene scrolls on for a couple of wheel ticks, fully in, before it is pinned; `0` for the hero (pinned from the top of the page) and the reviews (their conveyor is their own runway), `0.45` for the wizard, and none for a scene that never leaves): it scrolls like a normal section until its bottom reaches the viewport bottom (sticky `top = 100svh − height`, measured with a ResizeObserver; above the mobile dock below `lg` via `--journey-dock`), then stays pinned while its parts leave; the incoming scene overlaps that tail by `OVERLAP` (a negative top margin applied after mount) and the hand-over spans that **whole** stretch — `1 − overlap` of a viewport of scroll: previous parts out in the first half, new ones in in the second (the first scene skips the "out" half, `lead`) — and the moment they have landed the scene scrolls on: **the wheel never moves nothing**. The incoming box is not held still either: `riseOffset` eases it up into place along `(1 − t³)/3` — it starts a third of the stretch down, is as good as still while the previous parts leave, rises with growing speed as its own parts land and is at exactly scroll speed when it is released into normal flow, so there is no still frame and no jolt. Pacing is one number per journey (`ScrollJourney overlap`, default 0.35 — a bigger overlap is a quicker change): the hero → wizard opening runs `0.5` (half a viewport of hand-over), the journey after the projects runs **quick** at `0.7` (0.3 viewport: a section changes within about three wheel ticks, and a screen-high section costs ~0.5 viewport of scroll including its reading room). A journey can also carry **backdrop furniture** on `JourneyBackdropLayer` — a **fixed**, viewport-sized canvas under the scenes (first in the journey's `z-10` stacking context, so it paints over whatever the journey overlaps), faded in from `prelude` viewports before the first scene starts arriving (`approach`, off a zero-height head marker) and out as the tail marker clears the fold (`exit`, half a viewport — the footer coming up under the last scene). Its `position` = the sum of the scenes' arrivals (`JourneyRegistryContext`; scene k fully in at k + 1) + `approach` − 1, so it runs −1 → 0 over the prelude. Two kinds of furniture, both in `ui/journey-backdrops.tsx`: per-scene `backdrop` render functions (`JourneyBackdrop`, given `{ presence (0 → 1 → 0), arrive, depart }`) — `linesBackdrop` (reviews: two full-height vertical lines near the edges, 8 / 92 %, 3 / 97 % on phones, with small glowing particles floating around them (`Particle`, the `journey-drift` keyframe with per-dot drift / timing) and a sparser, dimmer field of them across the background — the lines' end fade is a mask on the 1px line itself, never on the wrapper, or it would clip the particles; they take the morph's sides over at the end of the arrival and hand them back at the start of the departure), `atomsBackdrop` (trainings: small "atoms" — a glowing nucleus with a hairline round orbit — drifting across the background, `Atoms look="ring"`), `wavesBackdrop` (contact: the three inner arcs + glowing dot behind the details column, inside the morph's ring), `servicesBackdrop` (the wizard / services step: the left blob, a soft dotted field behind the cards kept to the middle by a radial mask, and drifting glow particles), `blobLeftBackdrop` (spare), `blobRightBackdrop` (FAQ: the blob plus smaller atoms with tilted elliptical orbits, `look="orbit"`) — and the journey-wide `morph` (`journeyMorph`): ONE div driven by `position` through fixed stations (`stations()`, viewport px per breakpoint; geometry lerped with easing, looks: the stroke and the frame tint are `useMotionTemplate` colour-mixes of the brand tokens on the box itself, while the OSMO radial + green shadow and the ring's radial + purple glow are full-strength children faded with `opacity` — each its own compositor layer, so the shape moving across the screen never re-rasters those big blurred shadows): during the prelude it is the **OSMO copy circle** exactly as the showcase draws it (`osmoCircleAt`: same centre / size, its radial green look + soft shadow), then it **slides to the middle** and settles at the ring's size while the green drains to the page ground and the purple outline comes up — the **ring behind the stats** (stroke + radial + dashed inner ring), stretches into a rectangle taller than the viewport whose sides are the **two lines beside the reviews**, closes into the rounded **frame behind the training cards** (faint tint), travels to the **outer signal ring** behind the contact details, and shrinks away for the process (which draws its own wave). The home page is a server component, so it only hands these client references over — never call them there; it passes `prelude={OSMO_HANDOFF_VH}` (a plain-module number). A scene can slow its own stop with `pace={{ overlap }}` (none does today — the stats used to, and it read as too slow). `JourneyContext` also exposes `travel` (the whole held stretch) and `within` (the scene's own scrolling, 0 → 1 from top-at-top to bottom-pinned) via `useJourney()` for section-level choreography: the reviews' conveyor (`reviews-section.tsx` — on the home journey every review sits in **one close-packed grid** (two columns from `md`, the odd last card centred across both) that rides a sticky stage = the viewport under the header lane (`STAGE_TOP` 112px — the header pill ends ~84px down, so the heading never sits under it; the scene passes `pt-0`) and above the mobile dock, heading included; rows get at least a third of the window (`grid-auto-rows: minmax(…)`, capped at `ROW_MAX`) so **three rows fill it**, and scrolling slides the whole stack up through soft edges (`EDGE`, a mask): the top rows leave, the next appear from below — nothing fades per card. The runway is the stack's travel at `SPEED` 1.2× (measured with a ResizeObserver; `useScroll` on the runway, `start 0px` → `end 100%`, i.e. the stuck stretch on desktop and up to the hand-over below `lg`), so the last row is in place the moment the scene lets go; the cards are the `compact` `Testimonial` variant, their initials on **varied rounded shapes** (`ui/testimonial.tsx`: `AvatarShape` = circle · octagon · square · hexagon · diamond · pentagon, drawn as SVG with a fine round-joined stroke (4px in the 64-box) so every corner is rounded, plus a 3px halo in the page colour; **formal tints**, not solid colour — the shape is a light tint of the review's colour edged a little deeper with the letters in the colour itself, and in the dark theme a deeper tint over the dark card with a paler ink; the tints are mixed in JS (`avatarTints`) and handed over as CSS variables the wrapper's `dark:` classes pick from, because Tailwind v4 rewrites a `color-mix()` declaration into a nested `@supports` block that the dev pipeline dropped for a plain custom-property rule; names `text-lg`/`text-xl` and 19–22px rating stars; the reviews cycle `AVATAR_SHAPES` by index — some repeat on purpose — and a card outside a list falls back to a shape picked from its name). Elsewhere the marquee columns remain) and the process wave, which draws over the second half of the arrival so it reaches the 4th badge as the scene is released to scroll on. Off-stage scenes hide with `opacity: 0` (not `visibility: hidden` — Chrome then never lazy-loads their images) + `inert`. Part motion goes through `useScrollEased` (raw while Lenis glides the wheel, a stiff `useSpring` under native scrolling so wheel steps glide); the scene boxes and every `JourneyItem` are `will-change` compositor layers, so the scroll-linked rise / fade never repaints the sections; a whole-box fade (`sceneOpacity`) covers whatever a section does not mark (dividers, lines). The backdrop `position` is summed from the scenes' arrivals on the **next frame's pre-update step** (`frame.preUpdate`) — an arrival is a derived value that changes in the pre-render step, and a `useTransform` that has already been recomputed in that step is not recomputed again, so a synchronous sum left the morph a step behind after every jump. Progress is read off zero-height flow markers before/after each sticky box (a pinned element's own `offsetTop` moves with the pin, so nothing must measure the box itself) through `useFlowProgress` (no DOM read per frame); the same fact drives `scrollToElement` for a hash inside a scene (the header's Contact link): the box carries `data-journey-scene` + `data-journey-overlap`, and the scroll lands at its start marker's position less the overlap — where the box's top meets the viewport top and the scene is fully in (from below the section, the pinned box's own rect used to stop the scroll a few px away); the incoming's arrival and the outgoing's departure coincide because the scenes are adjacent. Transforms are gone once a scene is in (forms, accordions unaffected); off-stage scenes are `visibility: hidden` + `inert`. Until mount (server HTML, no JS) the sections are plain flow without overlap; reduced motion keeps them that way. `ScrollJourney` (`relative z-10 flow-root`) clones each scene with `isFirst` / `isLast` / `overlap` / `leaves`: by default the first scene overlaps whatever precedes the journey (`overlapFirst`, off for the hero at the top of the page) and the last scene never leaves (it scrolls straight into the footer) — `leaveLast` lets it leave when a non-journey block (the projects stage) follows instead |
| `src/components/footer.tsx` + `review-badges.tsx` + `src/lib/contact-info.ts` | The footer: brand (logo, line, language toggle) · links (Portfolio · Projects · Training · Contact) · **contact** (`EMAIL_PRIMARY`, `PHONE_PRIMARY`, `PHONE_SECONDARY`, the office address (`contact.addressVal`) behind a map pin linking to the place on Google Maps (`GOOGLE_REVIEWS.mapUrl`), the `SOCIAL_LINKS` icons) · **reviews** — `GoogleReviewsBadge` (the listing's rating + count from `GOOGLE_REVIEWS`, kept **by hand** — read off the place page, no key-less live source; links to the reviews on Google via the place id) and `ClutchBadge` (the official Clutch widget: `next/script` loads `widget.clutch.co/static/js/widget.js` lazily and `CLUTCHCO.Init()` fills the `.clutch-widget` div with an iframe — called from the script's `onLoad` and again on mount for client-side navigations, only while the container is empty; two containers, the dark one on the widget's `darkbg` variant toggled with `dark:`, with `color-scheme: light` on it because Chrome paints an opaque white canvas behind an iframe whose colour scheme differs from its embedder's) — with the legal links and the copyright along the bottom |
| `src/components/ui/` | Reusable primitives (shadcn-style) — put new primitives here |
| `src/lib/i18n/` | `config.ts` (server-safe LOCALES/dictionaries), `language-context.tsx` (client hook), `bg.ts` + `en.ts` dictionaries |
| `src/lib/use-media-query.ts` | `useMediaQuery(query)` and `useHydrated()` on `useSyncExternalStore` — client-only facts without an effect + `setState` (the server and the hydrating render see `false`, the first client render after hydration the real value); the journey scenes' `mounted` flag and Boleron's composite-or-plain-clip choice use them — never `useEffect(() => setState(...))` for these (the `react-hooks/set-state-in-effect` rule), and never read a ref during render (the journey registry is `useState`-initialised, not a ref) |
| `src/lib/routes.ts` | Localized path helpers (`homePath`, `servicePath`, …) — always use these, never hardcode paths |
| `src/lib/services/constants.ts` | Service identity: icon path ↔ URL slug maps (`SERVICE_SLUG_BY_ICON`) |
| `public/llms.txt` | Company/services brief for AI assistants — keep in sync with real offerings |

## 4. Rules (must-follow)

### Localization (bg primary, en secondary)
- **Every user-facing string lives in BOTH `src/lib/i18n/bg.ts` and `en.ts`.** No hardcoded UI text in components. The `Dictionary` type is inferred from `en.ts`, so the build fails on locale drift — keep shapes identical.
- When adding any feature, add translations for **all active locales in the language toggle** (currently bg + en). Bulgarian copy must read native — no machine-translation tone, no AI clichés.
- New pages: `generateStaticParams` from `LOCALES`, `dynamicParams = false` (unknown locales → 404), `generateMetadata` with per-locale title/description + `canonical` + `localeAlternates()`.
- Review content (names, roles, initials, review text) is localized too — it lives in the dictionaries under `reviews.items`.

### Reusable components
- Build components so they can be **added/removed from a layout without breaking it**: each home section is a self-contained `<section>` owning its own container/spacing (`mx-auto max-w-7xl px-4`), consuming the dictionary via `useLanguage()`, with an optional `className` prop.
- Primitives go in `src/components/ui/`, TypeScript-first, styled with Tailwind, following the existing shadcn conventions. Prefer reusing/extending existing components over duplicating.

### SEO & AI search
- Exactly **one keyword-rich `<h1>` per page** (home uses an `sr-only` h1; the visual hero headline is a `<p>`).
- Keep structured data in sync: Organization/WebSite (layout), Service + BreadcrumbList (service pages), FAQPage (FAQ section).
- New indexable routes must be added to `src/app/sitemap.ts` (`PATHS`) and get breadcrumb labels in `page-breadcrumbs.tsx` (`segmentToLabel`).
- Keep `public/llms.txt` and dictionary SEO fields (`seoTitle`/`seoDescription`) current. Never describe DreamTeam as an "agency".

### Dates
- **All dates shown to users — UI, emails, documents — use the `DD-MM-YYYY` format** (e.g. `05-09-2026`). Internal storage/transfer can stay ISO (`yyyy-mm-dd`); convert at the display boundary.

### Theming & visual conventions
- Page background (`ui/gradient-blur-bg.tsx`, fixed `z-[-1]` on every page): light theme is an **off-white** ground (`#f8f8f7`) with only faint tints of the brand red -> violet gradient plus a little grain (`.bloom-field-gradient` in `globals.css` — keep the blobs subtle); dark theme is the slate grid + magenta/violet orb (`.page-dark-grid-layer`). `ui/background-gradient-glow.tsx` (aurora) and `ui/grid-pattern.tsx` are unused primitives.
- **Dark is the default theme.** Dark text-card surfaces use `bg-card` (near-black slate `#0c111e`; light theme `#f7f8fb` — both defined once in `globals.css`). **Interactive surfaces** (selectable cards, dropzones, datepickers, slider boxes, form fields) use `bg-card-elevated`, which is *derived* via `color-mix()` from `--input`/`--background` over `--card` so retunes can't desync it; the nav/footer glass (`.liquid-glass*`) is likewise `color-mix()`-derived from `--card`, and standard card borders use `border-card-border`. Never hardcode dark card backgrounds (`dark:bg-neutral-950` etc.) — use these tokens so future cards inherit the standard.
  - A block can be **locked to one theme** whatever the visitor's — `theme-light` / `theme-dark` on it (the case studies' `StoryShell theme`): `globals.css` defines the `dark` variant as `&:is(.dark *, .theme-dark, .theme-dark *):where(:not(.theme-light, .theme-light *))` and declares the palette tokens for `:root, .theme-light` and `.dark, .theme-dark` alike (each with its `color-scheme`), so every `dark:` utility and every token inside a locked block follow the lock, not the `<html>` class (the exclusion sits in a `:where()`, so the variant's specificity is unchanged). A plain `.dark …` rule in `globals.css` that reaches into page content must be written the same way — `.dark .x:where(:not(.theme-light *)), .theme-dark .x` (`.shadow-input-soft` is); the glass / page-background ones only dress the chrome and need not.
  - `src/app/layout.tsx` renders `<html className="dark" style={{colorScheme:"dark"}}>` **server-side**. Keep it: `:root` is the light palette and next-themes' script runs from `<body>`, so without it the light theme flashes on every cold load (visibly, because `body` animates `background-color`). `defaultTheme="dark"` + `enableSystem={false}` handle the client side.
- Section headers: **centered** `h2` (`text-[2.75rem] leading-[1.06] font-extrabold sm:text-5xl md:text-6xl font-heading`, wrapper `text-center`) with an accent word in `text-section-accent`, subtitle `mx-auto max-w-2xl text-lg text-muted-foreground`. Page headers (`h1` on `/services`, `/projects`, …) stay left-aligned.
- Fonts: the whole site is set in **Exo 2** — body / UI (`--font-sans`) and headings (`--font-heading`, the `font-heading` utility) alike; it is the Plasico headline's face, which the client asked to have everywhere. **Self-hosted through `next/font/google`** in `layout.tsx` as a variable font (100–900 + italic, no weight list) with the `cyrillic` subset; its `--font-exo` variable lives on `<html>` and `globals.css` maps both tokens to it — `--font-sans` / `--font-heading` resolve on `:root`, so defining them on `<body>` silently drops the family back to the Tailwind default (and a token pointing at an undefined `--font-*` variable does the same, invisibly). Sofia Sans / Fira Sans are gone; Uni Sans (`src/app/fonts/`) is no longer wired up. The projects showcase adds the other four display faces (Montserrat, Playfair Display, Manrope, Unbounded — see the showcase row) as `--font-*` variables on `<html>` too, and MindGuard's page adds the platform's own two (Space Grotesk, DM Sans — Latin only, `--font-space-grotesk` / `--font-dm-sans`, Manrope behind them for the Cyrillic). **`font-heading` is inlined** by `@theme inline` — the utility carries the Exo 2 stack itself, not `var(--font-heading)` — so a subtree cannot re-point it through the variable: a page with its own face puts the family class on its headings (`PROJECT_DISPLAY_FONT[id]`, MindGuard's `HEADING`) and hands `StoryShell font` its body face. Turbopack once missed a `globals.css` write that landed in the same instant as a `layout.tsx` one — the served CSS kept the old tokens until the file was touched again; check the served chunk (`--font-sans:` in it) before chasing a ghost.
- Portfolio "All" tab (on `/portfolio`) is a **hand-picked highlight reel** (`embedsForAll()`), not every clip. Category tabs show the full catalogue; `?category=<key>` pre-selects a tab. The sticky controls (the category chips, the format tabs under them) sit `lg:top-[6rem]` on desktop — a 12px gap under the header pill (`7.25rem` left a 32px one the client found too much) — with the chips a step smaller there than on a tablet (`lg:h-9`, 20px icons — `CATEGORY_ICON` —, 13px labels) and a slimmer format pill (`lg:py-1`, `min-h-7`, 14px icons).
- Partners (`partners-section.tsx`) render as two rows inside the hero (`layout="static"`, `rows={2}`), the „доверяват ни се“ label sitting between two hairlines: from `lg` the rows are **static and centred, one line each** (`staticImgClass` — logos 2.5rem tall at `lg`, 3rem at `xl`, 3.25rem from `2xl`, width-capped 116 / 140 / 160px so eight fit a 1024px laptop with room; measured single-line from 1024 to 1920), and below `lg` — where they would wrap into a pile — the same two rows run as **marquees in opposite directions** (both variants are in the DOM, toggled with `lg:hidden` / `hidden lg:flex`; the hero pads the strip `pb-[5.5rem]` below `lg` so both rows clear the mobile dock). The `marquee` layout (default) is the scrolling variant everywhere — compact logos (~2.75–3.25rem tall, `mx-2.5`/`mx-3.5` spacing), a slow CSS `marquee-x` loop (70s, `--marquee-duration`, `[animation-direction:reverse]` for the second row) that **pauses while the row is hovered** (`group/marquee`) with the hovered logo growing to 125% (`transition-transform duration-300`). Keep `sizes` on the logos in step with their max rendered width. Mindguard leads the list (iSupport — the partner and its generic-layout project — was removed at the client's ask, its logo files with it); a partner with only a white-ink file sets `invertOnLight`; `href` is the client's site — the marquee links it, and so does the case study's hero logo (`ClientSite`); Asia Event Agency, Hubchev Properties, Valtcan and Designed by GG are commented out (parked) in `partners.ts`.
- The hero video is the Bunny Stream film `HERO_VIDEO` (`lib/bunny-stream.ts`, library 750681 — the DreamTeam ad `d856fe05-…`, the client's pick; swap the id there) in a muted, looping, cover-fit `bunnyBackgroundEmbedSrc` iframe (the YouTube `HERO_EMBED` is gone; the share link form is `player.mediadelivery.net/play/<library>/<guid>`). Hero copy sits bottom-left over the video on the `.hero-wash` overlay (`globals.css`, driven by `--background` so it is white in light and slate in dark — kept **light**: solid only at the very bottom so the hero melts into the page, most of the video shows through; the copy block sits tight above the partners strip, `pb-1`…`lg:pb-3`). The headline is set in the **Plasico display style** (`PROJECT_DISPLAY_FONT.plasico` — Exo 2 bold, uppercase; the client's pick for the page's big letters) and **white in both themes** (`text-white` + a soft dark text-shadow; the wash keeps it legible) and its glow word (`hero.titleGlow`, `.text-hero-accent`) gets a real red → violet **gradient glow**: three blurred copies of the word in `text-section-accent` (`HERO_GLOW_LAYERS`) sit behind the white letters. The description is **one line that inspires and says what we do** (`hero.subtitle` — „Ако можете да си го представите, ние можем да го създадем - AI видео и медийна продукция за вашия бранд.“ / "If you can imagine it, we can deliver it - AI video and media production for your brand.") in a **pale violet** (`#E4DBFF`, the brand's violet end lightened, with a soft dark text-shadow) rather than the muted grey. The section's bottom drop shadow is the page colour of each theme (white / the dark slate) so the hero melts into the page.
- Desktop header (`site-header.tsx`) is a floating glass pill (`top-4`, 96% wide, rounded-full, drop shadow) like the mobile bar; nav links — Portfolio · Projects · Services · Training · Contact (Projects is a dropdown like Services / Training: every case study in `PROJECTS` with the client's logo on a **white** tile (`projectItems`, `DropdownItem.tile`; a white-ink-only mark is inverted for it) and the name, the label itself the `/projects` listing; the mobile dock links Projects too); Contact is `homePath#contact` — the home page's contact form, a smooth scroll on the home page itself, a jump from any other page — flow inline below `2xl` (scrolling sideways) and are absolutely centred from `2xl` up (the bigger right-hand controls would collide with a centred nav on 1280–1535px screens); Services and Training are hover/focus dropdowns (`NavDropdown`, panel on the same `liquid-glass-header` surface + deep shadow, 56px image tiles from `services.items` icons and `useTrainingCards()` photos, sources requested at 224px); right side = theme toggle, two plain round icon buttons that **copy** the email / phone (`EmailCopyButton` / `PhoneCopyButton` in `phone-copy-button.tsx` — `EMAIL_PRIMARY` / `PHONE_PRIMARY` from `src/lib/contact-info.ts`; no hover choreography, a check + a small „Копирано!“ tag is the only feedback, `mailto:`/`tel:` fallback when the clipboard is blocked), the primary quote CTA — the projects' arrow-disc pill (`ButtonWithIcon`, `surface="auto"`, the slim `sm` size, `h-9`) at the bar's right end (end padding `pe-3` — a little more room than its `py-1.5`) under a **brand-gradient glow** (`glow`: a blurred red → violet halo wrapped around the pill, a little stronger on hover; the only CTA with one). Every quote CTA on the site (header, hero, service cards, mobile sheet, the Emblema story) is that same pill, reading „Поискай оферта“ / "Request a quote". The bar stays slim: logo `h-9`/`h-10`, the round copy buttons `size-9` with 18px icons — exactly the CTA's height (`h-9`) (the mobile bar is the roomier one — 96% wide up to `max-w-lg`, `py-2.5`, logo `h-10`, `size-10` round buttons — and links the icons instead — `EmailIconLink` / `PhoneIconLink`), row `py-1.5`, nav `text-[0.9375rem]` → `xl:text-base` → `2xl:text-lg` (a step under the old ramp), every link's letters filling with the **brand's red → violet gradient on hover** (`NAV_INK`: the gradient sits under the text, `bg-clip-text`, and shows as the colour fades to `text-transparent`; a plain link lifts a pixel with it, a dropdown's label keeps it while its menu is open (`group-aria-expanded`) and its chevron turns `primary`). Mobile keeps the dock (Projects · Contact · Services + menu; Portfolio is in the sheet) with plain links, no dropdowns; its menu button is a `3.25rem` gradient circle with a 26px icon, a little in from the dock's right edge (`me-1`). **Mobile scene snapping** (home page, the projects stage only): on coarse-pointer screens below `lg` the root snaps to zero-height `SnapStop` markers (`ui/snap-stop.tsx` — `scroll-snap-align: start` + `scroll-snap-stop: always`, so one swipe lands on the next stop, TikTok-style). On phones the scenes also **hand over early** — `SHOWCASE_HOLD_MOBILE` 0.3 instead of the desktop hold, passed into `sceneFrame` / the morph as `hold` (`useMediaQuery` on the same coarse-pointer query) — so the hand-over spans most of the swipe between two stops and the next scene moves with the finger, reels-style (the last scene's outro keeps `SHOWCASE_HOLD`). The stops are the stage's intro and each scene **fully framed with its copy revealed** (`SCENE_FRAMED` = scene-local t 0.27, past the CTA's reveal at 0.24 and before the OSMO copy fades at `OSMO_OUTRO_START` 0.3 — the same positions the rail jumps to, `scrollTargetFor`; rendered as `svh` offsets inside the section). The rule in `globals.css` keys off `main[data-snap-sections]` and uses `y proximity`, not `mandatory`: the stops are half a viewport apart, so inside the stage it always snaps, while the rest of the page (hero, wizard, the journey) scrolls free — it only snaps within about a third of a viewport of a stop, so a swipe out of the OSMO scene leaves cleanly and the journey keeps its smooth scroll choreography. Off under reduced motion and while a field has focus (the keyboard's scroll-into-view is not undone). While it is on, `scrollToElement` / `scrollToY` scroll natively (`sectionSnapActive()`): the browser re-snaps after any scroll, and would do so on every frame of one Lenis animated itself. A drag longer than the gap between stops still passes stops (only flings are held to one), so ordinary flicks advance one scene at a time. Anything that can sit at the bottom of a mobile viewport (e.g. the projects showcase rail) needs `pb-24` below `lg` to clear the fixed dock. Both mobile bars hug the screen edges to leave the most room: the top bar sits at `max(0.5rem, safe-area-inset-top)`, the dock at `max(0.375rem, safe-area-inset-bottom)`, both 96% wide up to `max-w-lg`; showcase copy blocks anchor `bottom-[5.5rem]` above the dock.
- `MAIN_WITH_FIXED_PAGE_BG_CLASS` uses `overflow-x-clip`, never `overflow-x-hidden` — `hidden` turns `<main>` into a scroll container and silently breaks every `position: sticky` inside it (projects showcase, portfolio controls).
- Service cards (`ui/service-card.tsx`, the wizard's first step and `/services`) are `rounded-[1.75rem]` surfaces with a breath of tint per service (`SERVICE_CARD_VARIANTS` = blue (AI video) · amber (mascot) · coral (AI images) · green (automation)): **near-white with dark ink in the light theme, the same tint over the dark card ground with white type in the dark theme** (the mascot card's dark surface is a violet tint — the playful, character-ish fourth hue next to blue / coral / green). The card itself sits still (no tilt, no lift — a moving card under the pointer made the CTA's hover flicker); only the **picture is 3D**: it tilts toward the pointer and comes forward while the pointer is over the card (motion springs on the image wrapper, its own `perspective`; nothing on touch / reduced motion). Layout: picture **right** (42%, on top on phones — `flex-row-reverse` keeps it first in the DOM), title on the left and, at the bottom, the "learn more" link **above** the arrow-disc quote pill (`ButtonWithIcon surface="auto"`). `href` turns the "learn more" label into a real link (the service's page) **and makes the whole card its hit area** (a click anywhere but on the pill / link does `router.push`; the link stays the real anchor for keyboard and right-click), `cta` adds the quote pill (a `<button>`, so the card itself must not be wrapped in a link then) - the `/services` page renders `ServiceCard` without either, wrapped in a `Link`. `ui/3d-card.tsx` is gone.
- Service pages (`services/service-detail-page-view.tsx`, `/services/[slug]`): the `FeatureShowcase` hero carries the main CTA a size up (`ButtonWithIcon size="lg"`, `action` prop) that scrolls to `#quote`, and the page ends with the **quote wizard opened straight on that service's flow** (`QuoteFormSection initialService`, step 1 of the flow; its "change service" back button still reaches the cards) instead of the old contact form.
- **Every button — and every list/menu of buttons — gets a hover animation**, not just a colour change: a transform (lift, scale, nudge) plus a shadow or background shift, on a `transition-[...] duration-200 ease-out`. Examples to copy: `primaryGradientInteractiveClassName` (scale + deepening glow), the nav dropdown rows (`hover:translate-x-1` + tile scale + shadow), the category chips (`hover:-translate-y-[1px]`). Disabled states keep `cursor-not-allowed` and no motion.
- In a `cn()` call a `leading-*` class must come **after** every font-size class — `text-3xl`, `text-[clamp(…)]`, a size ramp passed as a later argument — because tailwind-merge drops a leading that precedes a font-size (Tailwind's sizes carry a line-height); the story headings, the hero titles and the showcase headline all lost their tight leading to this once.
- Interactive elements always get `cursor-pointer` (unless disabled). Breadcrumbs on all non-home pages (automatic via `PageBreadcrumbs`). Modals: full-viewport on mobile with a top-right X close.
- Images: use `next/image` with a `sizes` hint (qualities 75/100 are configured). Raw `<img>` only with an explicit reason.

### Forms & email
- Contact/training inquiry forms POST to `/api/contact` (includes the internal Bulgarian form label `formStateBg`); the home quote wizard POSTs multipart to `/api/quote` (attachments + option-keyed answers). Both send via Resend **to `info@dreamteam.technology`**, render HTML + plaintext, and share the hygiene layer in `src/lib/server/form-guards.ts`.
- Server-side validation is the contract: required name/email/message, length caps, honeypot (`website` must stay in forms, hidden), per-IP rate limit. Check `result.error` from Resend — never report success on failure.

### Quality gates
- `npm run build` **and** `npm run lint` must pass before finishing any change.
- If you ran `next build` locally, delete `.next` before `next dev` — stale production manifests cause dev 404s on dynamic routes.
- Keep changes minimal, production-ready, consistent with repo style. Report failures honestly.

## 5. Environment & deployment

- `.env.local`: `RESEND_API_KEY` (required for email), `RESEND_FROM` (optional, defaults to `info@dreamteam.technology`)
- Scripts: `npm run dev` / `build` / `start` / `lint`
- Hosted on Vercel; permanent redirects + security headers live in `next.config.ts` (retired `/zh` locale → `/en`)
